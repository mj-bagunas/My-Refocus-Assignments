// Create a variable named “width” using the “const” declaration.
// Set the value of “width” as length = 20 width = 16
// Create a variable named “length” using the “const” declaration
// Set the value of “length” as 20
// Calculate the area

const width = 16;
const length = 20;
let area = length * width;

console.log(`Width:${width}`);
console.log(`Length:${length}`);
console.log(`Area:${area}`);
